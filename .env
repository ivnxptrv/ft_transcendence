# shellcheck disable=SC2034

BACKEND_HOST=localhost
BACKEND_PORT=4009
BACKEND_URL=http://${BACKEND_HOST}:${BACKEND_PORT}

# --- Identity Service ---
IDENTITY_HOST=localhost
IDENTITY_PORT=4010
IDENTITY_URL=http://${IDENTITY_HOST}:${IDENTITY_PORT}

# --- Ledger Service ---
LEDGER_HOST=localhost
LEDGER_PORT=4011
LEDGER_URL=http://${LEDGER_HOST}:${LEDGER_PORT}

# --- Semantic Service ---
SEMANTIC_HOST=localhost
SEMANTIC_PORT=4012
SEMANTIC_URL=http://${SEMANTIC_HOST}:${SEMANTIC_PORT}

# --- Interaction Service ---
INTERACTION_HOST=localhost
INTERACTION_PORT=4013
INTERACTION_URL=http://${INTERACTION_HOST}:${INTERACTION_PORT}
