# Favicon Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the default Next.js favicon with a two-speech-bubble icon in dark and cream variants.

**Architecture:** Two files in `src/app/` — a static `icon.svg` (dark, served as the browser tab icon) and a code-generated `apple-icon.tsx` (cream, rendered at build time via `ImageResponse`). The existing `favicon.ico` is deleted because `icon.svg` supersedes it in Next.js 16.

**Tech Stack:** Next.js 16 file conventions (`icon`, `apple-icon`), `next/og` `ImageResponse`

---

## File Map

| Action | Path | Purpose |
|--------|------|---------|
| Delete | `src/app/favicon.ico` | Replaced by icon.svg |
| Create | `src/app/icon.svg` | Dark variant — browser tab favicon |
| Create | `src/app/apple-icon.tsx` | Cream variant — Apple touch icon (PNG via ImageResponse) |

---

### Task 1: Delete the default favicon and add the dark SVG icon

**Files:**
- Delete: `src/app/favicon.ico`
- Create: `src/app/icon.svg`

- [ ] **Step 1: Delete `favicon.ico`**

```bash
rm src/app/favicon.ico
```

- [ ] **Step 2: Create `src/app/icon.svg`**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 40 36">
  <!-- background -->
  <rect width="40" height="36" fill="#0a0a0a"/>
  <!-- client bubble (top-left) -->
  <rect x="1" y="1" width="22" height="14" rx="4" fill="#faf9f7"/>
  <polygon points="4,15 11,15 4,21" fill="#faf9f7"/>
  <!-- insider bubble (bottom-right) -->
  <rect x="17" y="19" width="22" height="14" rx="4" fill="#b0a898"/>
  <polygon points="36,33 29,33 36,39" fill="#b0a898"/>
</svg>
```

- [ ] **Step 3: Start the dev server and verify the favicon appears in the browser tab**

```bash
npm run dev
```

Open `http://localhost:3000` in a browser. The tab should show the dark two-bubble icon instead of the Next.js logo. If the old favicon is cached, hard-refresh (`Ctrl+Shift+R` / `Cmd+Shift+R`).

- [ ] **Step 4: Commit**

```bash
git add src/app/icon.svg
git rm src/app/favicon.ico
git commit -m "feat: add dark speech-bubble favicon"
```

---

### Task 2: Add the cream Apple touch icon

**Files:**
- Create: `src/app/apple-icon.tsx`

- [ ] **Step 1: Create `src/app/apple-icon.tsx`**

```tsx
import { ImageResponse } from "next/og";

export const size = { width: 32, height: 32 };
export const contentType = "image/png";

export default function AppleIcon() {
  return new ImageResponse(
    (
      <div
        style={{
          width: "100%",
          height: "100%",
          background: "#ede9e3",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
        }}
      >
        <svg width="28" height="26" viewBox="0 0 40 36">
          {/* client bubble (top-left) */}
          <rect x="1" y="1" width="22" height="14" rx="4" fill="#3a3530" />
          <polygon points="4,15 11,15 4,21" fill="#3a3530" />
          {/* insider bubble (bottom-right) */}
          <rect x="17" y="19" width="22" height="14" rx="4" fill="#7a706a" />
          <polygon points="36,33 29,33 36,39" fill="#7a706a" />
        </svg>
      </div>
    ),
    { ...size },
  );
}
```

- [ ] **Step 2: Verify the build succeeds**

```bash
npm run build
```

Expected: build completes with no errors. Next.js will output something like:
```
Route (app)                  Size
├ ○ /                        ...
...
```
No red error lines. Warnings about `apple-icon` size are fine.

- [ ] **Step 3: Verify the `<head>` tags are correct**

Start the dev server and view page source (`Ctrl+U`):

```bash
npm run dev
```

Look for both of these in the `<head>`:
```html
<link rel="icon" href="/icon?..." type="image/svg+xml" sizes="any">
<link rel="apple-touch-icon" href="/apple-icon?..." type="image/png" sizes="32x32">
```

- [ ] **Step 4: Commit**

```bash
git add src/app/apple-icon.tsx
git commit -m "feat: add cream apple-touch-icon variant"
```
