events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        # Route everything else to the Frontend
        location / {
            default_type text/plain;
            return 200 "Nginx is alive - Microservices Gateway";
        }
    }
}
# worker_processes 1;

# events { worker_connections 1024; }

# http {
#     include       mime.types;
#     default_type  application/octet-stream;
#     sendfile        on;
#     keepalive_timeout  65;

#     # --- UPSTREAM DEFINITIONS ---
#     # These names must match the service names in your docker-compose.yml
#     upstream identity_api {
#         server identity-svc:8001;
#     }

#     upstream ledger_api {
#         server ledger-svc:8002;
#     }

#     upstream semantic_api {
#         server semantic-svc:8003;
#     }

#     upstream frontend_app {
#         server frontend:5173; # Assuming Vite/React default port
#     }

#     server {
#         listen 80;
#         server_name localhost;

#         # --- API ROUTING (Internal Endpoints) ---
        
#         location /api/auth/ {
#             proxy_pass http://identity_api/;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#         }

#         location /api/ledger/ {
#             proxy_pass http://ledger_api/;
#             proxy_set_header Host $host;
#         }

#         location /api/search/ {
#             proxy_pass http://semantic_api/;
#             proxy_set_header Host $host;
#         }

#         # --- MONITORING & LOGGING ---
        
#         location /kibana/ {
#             proxy_pass http://kibana:5601/;
#             proxy_set_header Host $host;
#         }

#         location /grafana/ {
#             proxy_pass http://grafana:3000/;
#             proxy_set_header Host $host;
#         }

#         # --- FRONTEND ROUTING ---

#         location / {
#             proxy_pass http://frontend_app;
#             proxy_set_header Host $host;
#             # Support for HMR (Hot Module Replacement) in React
#             proxy_http_version 1.1;
#             proxy_set_header Upgrade $http_upgrade;
#             proxy_set_header Connection "upgrade";
#         }
#     }
# }
